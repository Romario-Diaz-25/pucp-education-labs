/* eslint-disable @typescript-eslint/no-explicit-any */
export interface ITypeOptions {
  primaryKey: boolean;
  length: number;
  textType: string;
  precision: number;
  scale: number;
  values: Array<any>;
}
